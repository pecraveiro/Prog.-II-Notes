// Output em C++

#include <iostream> //bibliteca padrão - diretiva de inclusão

using namespace std; // std: standart library

int main()
{
    int x; // declaração de tipagem
    x = 10; // declaração da variável
    cout << x << endl; // printar na tela para o usuário
    
    return 0; 
}

// utilizamos o int (linha 5) para o RETORNO do SO (System Operator)
// endl - utilizamos para pular uma linha
// cout - utilizamos para printar
// << operador que dá a informação
// >> operador que recebe a informação
// operador: é uma função, por convenção ">> entrada" "<< saída"
